syntax = "proto3";

message Comment {
    string ID = 1;
    string GroupID = 2;
    string Text = 3;
    CommentState State = 4;
}

enum CommentState {
    CREATED = 1;
    UPDATED = 2;
    DELETED = 3'
}